import { Box, VStack } from "@chakra-ui/react";
import { Menu } from "../components/Menu";

const Home = () => {
  return (
    <VStack w="100vw" h="100vh">
      <Menu />
    </VStack>
  );
};

export default Home;
